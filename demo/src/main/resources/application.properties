spring.application.name=demo
spring.web.resources.static-locations=classpath:/static/
spring.datasource.url=jdbc:mysql://localhost:3306/spring?serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.username=bokng
spring.datasource.password=123123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.mvc.hiddenmethod.filter.enabled=true
server.servlet.session.timeout=300s
# HTTPS 환경 기준 secure cookie 설정. 로컬 http에서 로그인 유지가 깨질 수 있으므로 dev 프로필에서는 false로 조정하세요.
server.servlet.session.cookie.secure=true
# 업로드 파일 저장 경로 (없으면 실행 시 자동 생성)
spring.servlet.multipart.location=upload
